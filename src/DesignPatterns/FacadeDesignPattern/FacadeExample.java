package DesignPatterns.FacadeDesignPattern;

public class FacadeExample {
    
    /*
        used when we have ultiple interface, and to keep the call to the facade interface
        which provide better interface reading and routing to related interface

        its similar to the factory design pattern
    */
}
